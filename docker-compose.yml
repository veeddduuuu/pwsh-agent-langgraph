services:
  pwshagent:
    build: .
    image: pwshagent:latest
    container_name: pwsh-agent
    env_file:
      - .env
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: bash

